# JobTracker — Kod Oturumu Kuralları

Kaynak kod bu dizinde. Wiki (kalıcı bilgi tabanı): `C:\dev\vaults\vaultProject`

## Kod Değişikliği Sonrası (Aynı Yanıtta)

1. İlgili wiki sayfalarını güncelle:
   - Mimari değişikliği → `wiki/architecture/`
   - Bileşen değişikliği → `wiki/components/`
   - Özellik değişikliği → `wiki/features/`
2. `wiki/tasks/Mevcut_Tasklar.md` içinde tamamlanan görevi `[x]` işaretle
3. `log.md` başına yeni giriş ekle (`## [YYYY-MM-DD] change | açıklama`)

Kullanıcı sormadan bunu yap.

## Yasaklar

- API anahtarlarını koda yazma — yalnızca `.env`
- `log.md`'yi sıfırlama — yalnızca başa ekle
- Kod değişikliği teyit edilmeden görevi `[x]` işaretleme

## Tam Protokol

`C:\dev\vaults\vaultProject\CLAUDE.md` — şema, isimlendirme kuralları, log/index formatı
